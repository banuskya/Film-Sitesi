const arrows = document. querySelectorAll("-arrow");
const novieLists = document.querySelectorAll(".movie-list");

arrows.forEach((arrow, i) => {
    const ımageItem = movieLists[i].querySelectorAll("img").lenght;
    arrow.addEvenListener("click", function (){
    movieLists[i].style.transform= ``
    });

});